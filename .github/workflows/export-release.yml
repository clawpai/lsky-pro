name: Export Image to Release

on:
  workflow_dispatch:

env:
  IMAGE: ghcr.io/clawpai/lsky-pro:latest

jobs:
  export:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull image
        run: docker pull ${{ env.IMAGE }}

      - name: Export & compress
        run: |
          docker save ${{ env.IMAGE }} | gzip -1 > lskyPai-PRO-2.3.tar.gz
          ls -lh lskyPai-PRO-2.3.tar.gz

      - name: Create Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create image-export --title "镜像导出包" --notes "lskyPai-PRO v2.3 多架构镜像 tar.gz（docker load 导入）" || true

      - name: Upload asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload image-export lskyPai-PRO-2.3.tar.gz --clobber
          echo "=== 完成 ==="
          gh release view image-export --json assets -q '.assets[] | .name + " " + .size'